# OS
.DS_Store
Thumbs.db

# Local-only Claude Code settings (keep .claude/settings.json, which records the
# frontend-design plugin this project uses)
.claude/settings.local.json

# Editor
.vscode/
.idea/

# Scratch render mockups (finals live in assets/brand/social/)
build/

# LOCAL ONLY — never commit. The whole repo is served publicly by GitHub Pages,
# so internal/sensitive/pre-announcement material must stay off it.
CLAUDE.local.md
drafts-local/
assets/brand/social/*-copy.md
